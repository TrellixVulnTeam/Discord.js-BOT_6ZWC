const discord = require("discord.js");
const botConfig = require("../botconfig.json");
var prefix = botConfig.prefix


module.exports.run = async (bot, message, arguments) => {
    console.log("no help available yet")
}

module.exports.help = {
    name: "unk"
}